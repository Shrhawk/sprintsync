# SprintSync Production Environment Configuration
# Copy this file to .env for production deployment

# Database Configuration (defaults provided in docker-compose.prod.yml)
POSTGRES_DB=sprintsync
POSTGRES_USER=postgres
POSTGRES_PASSWORD=secure_postgres_password_here

# Application Database URL (auto-constructed from above if not set)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/sprintsync

# Security
SECRET_KEY=your-super-secure-secret-key-use-openssl-rand-hex-32

# API Configuration (CRITICAL: Must match backend expectation)
ALLOWED_HOSTS_STR=http://54.172.72.68:80,http://54.172.72.68,https://54.172.72.68,http://54.172.72.68:8000
VITE_API_BASE_URL=https://your-domain.com

# AI Configuration (optional)
OPENAI_API_KEY=your-openai-api-key-here

# AWS Configuration (for CloudWatch logging)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# Demo User Configuration (optional - defaults provided)
ADMIN_EMAIL=admin@sprintsync.com
ADMIN_PASSWORD=secure-admin-password
ADMIN_FULL_NAME=SprintSync Admin

DEMO_EMAIL=demo@sprintsync.com
DEMO_PASSWORD=secure-demo-password
DEMO_FULL_NAME=Demo User

# Notes:
# - All variables have sensible defaults in docker-compose.prod.yml
# - Only override what you need to customize
# - For local testing, most defaults will work fine
# - For production, change passwords and SECRET_KEY
